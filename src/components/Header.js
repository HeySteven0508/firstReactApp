import Button from './AddButton'
const Header = ({}) => {
    return (
        <div className="header">
            <h1> List of Students </h1> <Button />
        </div>
    )
}

export default Header
